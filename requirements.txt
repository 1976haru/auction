# ── HTTP / 환경 ──────────────────────────────────────
requests>=2.31.0
python-dotenv>=1.0.0

# ── 계산 / 시뮬레이션 ────────────────────────────────
numpy>=1.26.0

# ── 대시보드 ─────────────────────────────────────────
streamlit>=1.35.0
pandas>=2.2.0
plotly>=5.20.0
reportlab>=4.0.0

# ── 테스트 ───────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=4.1.0

# ── 선택: Claude API 호출 시에만 필요 (USE_AI=true) ──
# anthropic>=0.28.0

# ── 선택: 실제 법원경매 크롤링 시에만 필요 ───────────
# playwright>=1.44.0

# ── 선택: PDF 텍스트 추출 시에만 필요 ───────────────
# pdfplumber>=0.11.0

# ── 선택: 외부 영구 DB (Turso) 시에만 필요 ──────────
# libsql-experimental>=0.0.40
